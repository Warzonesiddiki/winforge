interface:
  display_name: "Chatbot"
  short_description: "Repo-specific patterns and workflows for chatbot"
  default_prompt: "Use the chatbot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true