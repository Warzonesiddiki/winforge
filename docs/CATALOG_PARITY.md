# WinForge Catalog Parity Report

Generated by tools/catalog_parity.py


## Summary

- Web tweaks: 64 · Engine tweaks: 219
- Engine tweaks needing metadata: 129
- Backfilled this run: 0
- Debloat gaps: 0 · App gaps: 0

- ✅ EQUIVALENT — web tweak `perf-ultimate-power` (Ultimate Performance Power Plan) is covered by engine tweak(s) `winforge-power-ultimate`
- ❌ GAP — web tweak `perf-pagefile` (Optimize Pagefile Management) has no engine equivalent
- ❌ GAP — web tweak `perf-memcompression` (Tune Memory Compression) has no engine equivalent
- ❌ GAP — web tweak `ui-classic-context` (Classic Right-Click Context Menu) has no engine equivalent
- ❌ GAP — web tweak `net-disable-netbios` (Disable NetBIOS over TCP/IP) has no engine equivalent
- ❌ GAP — web tweak `pwr-hibernation` (Disable Hibernation) has no engine equivalent
- ❌ GAP — web tweak `pwr-processor-mgmt` (Maximize Processor Power Management) has no engine equivalent
- ❌ GAP — web tweak `exp-classic-paint` (Restore Classic Paint/Notepad) has no engine equivalent

## Result: full parity achieved (2026-08-16)

| Catalog | Web app | Engine before | Engine after |
|---|---|---|---|
| Tweaks | 64 | 163 (141 without metadata) | **219** (56 web tweaks merged with native ops; 12 atlas-* entries backfilled with matching metadata) |
| Debloat packages | ~95 | 43 | **102** (+59 web families) |
| Applications | 64 | 52 | **83** (+31 web winget IDs) |

Coverage of the 64 web tweaks: **57 in the engine catalog** (56 merged directly + 1 equivalent via `winforge-power-ultimate`); 7 remain excluded for the documented reasons below.

### The 7 exclusions are deliberate (safety, not gaps)

| Web tweak | Reason it is NOT in the engine catalog |
|---|---|
| `perf-pagefile` | `PagingFiles` is a REG_MULTI_SZ value; a string/dword write would corrupt the pagefile configuration. |
| `perf-memcompression` | MMAgent cmdlets are PowerShell-only, and the engine's elevated executor **never invokes PowerShell** (allowlist security boundary — see `TestRunCommandElevationBoundary`). Needs a native winapi op. |
| `net-disable-netbios` | `SetTcpipNetbios` is WMI/CIM-only; same PowerShell boundary. Needs a native WMI op. |
| `pwr-hibernation` | `powercfg /hibernate` is not an allowlisted elevated command; the engine prefers native power APIs (a `power_hibernate` op is the future fix). |
| `pwr-processor-mgmt` | `powercfg -setacvalueindex` is not allowlisted; the engine already has native `power.SetProcessorState` — it needs a catalog op type to expose it. |
| `ui-classic-context` | Operates on a registry key's `(Default)` value; the engine registry layer does not support default-value writes. |
| `exp-classic-paint` | The web op is narrative text; Appx lifecycle is handled natively by the engine's bloatware catalog. |

### Mapping rules (mechanical, documented in tools/web_catalog_to_engine.py)

- `HKCU\path: Name = 0x…/n` → `registry_set_dword` · `Name = "text"` → `registry_set_string`
- undo `= default` → `registry_delete` (restores the Windows default)
- abbreviated paths (`…`) expanded only via a documented map (Explorer\Advanced, SearchSettings, PrefetchParameters)
- `powercfg`/`powershell` commands are **refused** (elevated-executor allowlist boundary); `bcdedit`/`fsutil` → `command`
- `Service A, B -> Mode` → one `service_start_mode` op per service; `Automatic (Delayed)` → `auto`
- `Scheduled Task: path -> Disabled/Enabled` → `task_disable`/`task_enable`
- risk `expert` → `high` (engine has no expert tier)
- `powercfg -duplicatescheme <GUID>` → matched against native `power_scheme` ops (alias-aware: `ultimate` ≡ `e9a42b02-…`)

### Verification after merge

- `go vet ./...` ✅ · **18/18 test packages** ✅ incl. `-race` — the strict config loader accepts the merged catalog
- `GOOS=windows go build` ✅ → **`winforge.exe` 6.31 MB** valid PE; windows test binaries compile
- Engine CLI smoke: `list` shows the merged tweaks; `scan` counts verifiable tweaks excluding one-way ops by engine design
- Catalog contains **zero** command ops outside the elevated-executor allowlist

### Keeping parity green (CI-ready)

```bash
python3 tools/catalog_parity.py --fix            # backfill metadata + sync debloat/apps
python3 tools/web_catalog_to_engine.py --apply   # re-merge web tweaks (idempotent)
python3 tools/catalog_parity.py --write-report docs/CATALOG_PARITY.md
```

Notes:
- 129 engine tweaks (atlas-* raw imports) still lack human names/descriptions; the parity tool backfills them only when a web op-signature match exists.
- Debloat/app descriptions added by the sync are factual category-level text; app descriptions derive from the web catalog.
