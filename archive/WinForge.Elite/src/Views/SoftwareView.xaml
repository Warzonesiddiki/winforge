<UserControl x:Class="WinForge.Elite.Views.SoftwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <ComboBox Width="180"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      Margin="0,0,8,0" />
            <Button Content="Install Selected"
                    Command="{Binding InstallSelectedCommand}"
                    Margin="0,0,8,0"
                    Padding="12,4"
                    FontWeight="SemiBold" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" IsEnabled="{Binding CanRefresh}" Padding="14,4" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom"
                   Text="{Binding StatusMessage}"
                   Opacity="0.6"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0" />

        <ListBox ItemsSource="{Binding Items}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Content="{Binding Model.Name}"
                                          IsChecked="{Binding IsSelected}"
                                          IsEnabled="{Binding CanInstall}"
                                          FontWeight="SemiBold"
                                          VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock FontSize="12" Opacity="0.6" Margin="22,1,0,0">
                                <Run Text="{Binding Model.Publisher}" /><Run Text=" · " /><Run Text="{Binding Model.Category}" /><Run Text=" · " /><Run Text="{Binding Model.Source}" />
                            </TextBlock>
                        </StackPanel>

                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="11" Margin="0,0,8,0" Text="{Binding StatusText}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#717171" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StatusText}" Value="Installed">
                                            <Setter Property="Foreground" Value="#107C10" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusText}" Value="Installing…">
                                            <Setter Property="Foreground" Value="#9D5D00" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding StatusText}" Value="Failed">
                                            <Setter Property="Foreground" Value="#C42B1C" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Button Grid.Column="2" Content="Uninstall" VerticalAlignment="Center" Padding="12,4"
                                Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}"
                                IsEnabled="{Binding CanUninstall}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanUninstall}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
