<UserControl x:Class="WinForge.Elite.Views.PrivacyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Privacy Score" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding PrivacyPercent, StringFormat={}{0:F0}%}" FontWeight="Bold" FontSize="16" Margin="10,0,0,0" />
                <TextBlock Text="{Binding EnabledCount, StringFormat={}{0} of }" Margin="8,2,0,0" Opacity="0.6" />
                <TextBlock Text="{Binding TotalCount, StringFormat={}{0} rules enabled}" Margin="0,2,0,0" Opacity="0.6" />
            </StackPanel>
            <ProgressBar Height="8" Maximum="100" Value="{Binding PrivacyPercent}" Margin="0,4,0,0" />
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Harden All" Command="{Binding HardenAllCommand}" FontWeight="SemiBold" Padding="14,4" Margin="0,0,8,0" />
                <Button Content="Refresh" Command="{Binding RefreshCommand}" IsEnabled="{Binding CanRefresh}" Padding="14,4" />
            </StackPanel>
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom"
                   Text="{Binding StatusMessage}"
                   Opacity="0.6"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0" />

        <ListBox ItemsSource="{Binding Items}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Model.Name}" FontWeight="SemiBold" />
                                <TextBlock Style="{StaticResource RiskBadgeStyle}" />
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Enabled" />
                                            <Setter Property="Foreground" Value="#107C10" />
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Setter Property="Margin" Value="8,0,0,0" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Model.Enabled}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Text="{Binding Model.Description}" TextWrapping="Wrap" Opacity="0.65" FontSize="12" />
                            <TextBlock Text="{Binding Model.Category}" Opacity="0.5" FontSize="11" />
                        </StackPanel>

                        <Button Grid.Column="1" VerticalAlignment="Center" Padding="14,4"
                                Command="{Binding DataContext.ToggleCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}"
                                IsEnabled="{Binding CanToggle}">
                            <TextBlock Text="{Binding ToggleText}" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
